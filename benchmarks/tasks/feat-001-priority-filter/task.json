{
  "id": "feat-001-priority-filter",
  "category": "feature",
  "difficulty": "medium",
  "title": "Add priority filtering to list command",
  "description": "The CLI list command supports --status but not --priority. Add --priority filter support to both the CLI and the underlying filter logic. The TaskStore.list() already has the filter field but the CLI doesn't expose it.",
  "repository": "task-service",
  "split": "train",
  "estimated_turns": 8,
  "hidden_test_command": "bun test tests/hidden/priority-filter.test.ts",
  "reject_criteria": [
    "must_not_modify_tests",
    "must_not_disable_typecheck",
    "must_not_introduce_any",
    "must_not_delete_existing_tests"
  ]
}
