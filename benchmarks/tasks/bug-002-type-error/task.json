{
  "id": "bug-002-type-error",
  "category": "bug_repair",
  "difficulty": "easy",
  "title": "Fix TypeScript type error in task update",
  "description": "The `update` method in TaskStore accepts an `UpdateTaskInput` but there's a type mismatch in the CLI handler when passing update fields.",
  "repository": "task-service",
  "split": "train",
  "seeded_defect": "The CLI update handler extracts fields from args but doesn't properly type them before passing to store.update().",
  "estimated_turns": 4,
  "hidden_test_command": "bun test tests/hidden/type-error.test.ts",
  "reject_criteria": [
    "must_not_modify_tests",
    "must_not_disable_typecheck",
    "must_not_introduce_any",
    "must_not_delete_existing_tests"
  ]
}
